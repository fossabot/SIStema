language: python
cache: pip

python:
  - "3.5"
  - "3.6"

install:
  - pip install -r src/requirements.txt

# Test all the core apps + all the apps within the modules directory
script:
  - python src/web/manage.py test dates frontend generator groups home questionnaire schools sistema users modules
