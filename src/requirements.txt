### This file contains requiremenets for SIStema project. Install them with `pip install -r requirements.txt`

### Django stuff
Django >= 1.8.4
Jinja2 >= 2.7.3
django-jinja >= 1.4.1
django-reversion

requests >= 2.7.0
